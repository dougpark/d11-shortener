# new admin page

## Section 1: Admin landing page
- create a new admin.html page with the following features:
  - show some site statistics
    - number of bookmarks
    - number of handles/users
    - number of api tokens
    - number of rss feeds 
    - what are stats would be useful for an admin to see at a glance?

# New link in user menu
- currenty show all the time, but eventually only show if user is admin
- link to /admin.html

## v2 will have sections/buttons for handle/user admin, api token admin, rss feeds admin, but v1 will just be read-only stats